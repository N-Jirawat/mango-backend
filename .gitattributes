# Treat Python files with Unix-style line endings to avoid issues on Windows
*.py text eol=lf

# Treat shell scripts with Unix-style line endings
*.sh text eol=lf

# Explicitly mark text files for consistent line endings
*.txt text eol=lf
*.md text eol=lf
*.yaml text eol=lf

# Binary files (ensure Git doesn't try to merge them as text)
*.h5 binary
*.keras binary
*.npy binary
*.pb binary